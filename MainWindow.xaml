<Window x:Name="MouseActionCaptureMainWindow" x:Class="MouseCapture.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MouseCapture"
        mc:Ignorable="d"
        Title="Mouse Action Capturer" Height="450" Width="800" FontFamily="Arial Black" FontWeight="Bold" Icon="/MouseClick.png">
    <Grid Background="#FFF1E5E5">
        <Grid.RowDefinitions>
            <RowDefinition Height="12"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="425*"/>
            <ColumnDefinition Width="49*"/>
            <ColumnDefinition Width="326*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="StartButton" Grid.Row="1" Grid.Column="0" Content="Start capturing" Background="#FF41DF1C" FontFamily="Arial" Click="StartBtn_Click" FontWeight="Bold" />
        <Button x:Name="StopButton" Grid.Row="1" Grid.Column="1" Content="Stop capturing" FontFamily="Arial" Background="#FFD4442B" FontWeight="Bold" Click="StopBtn_Click" Grid.ColumnSpan="2" />
        <DataGrid Margin="20,20,20,20" Grid.Row="2" Grid.ColumnSpan="3" AutoGenerateColumns="False" SelectionChanged="DataGrid_SelectionChanged"
          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" CanUserSortColumns="True" 
                  ItemsSource="{Binding MouseActions}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Action On" Width="200" Binding="{Binding ActionOn}" />
                <DataGridTextColumn Header="LMouse Action" Width="200" Binding="{Binding LMouseAction}" />
                <DataGridTextColumn Header="Application Name" Width="200" Binding="{Binding ApplicationName}" />
                <DataGridTextColumn Header="PID" Width="100" Binding="{Binding PID}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
